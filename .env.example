# URL base do backend (Fase 2). Vazio = app 100% local (dados de src/data/challenges.js).
# Setado (ex.: http://localhost:4000) = busca desafios/ranking do backend, com fallback
# automático para os dados locais caso o servidor esteja indisponível.
VITE_API_URL=
